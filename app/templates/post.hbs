<div class="col-md-10 col-center">
  <h1 class="post-title">{{model.post.title}}</h1>
  <h3 class=post-author>By: {{model.post.author}}</h3>
  <img class="post-image" src={{model.post.headerImage}}/>
  <div class="post-body">
    {{{model.post.body}}}
  </div>
  <hr>
  <h4>Comments:</h4>
  <div class="comments">
    {{#each model.post.comments as |comment|}}
      {{single-comment comment=comment}}
    {{/each}}
    {{#if session.isAuthenticated}}
      {{new-comment saveComment="saveComment" post=model.post user=model.user}}
    {{else}}
      {{#link-to 'sign-in'}}
        <button class="btn btn-success btn-block">
          Sign in to comment
        </button>
      {{/link-to}}
    {{/if}}
  </div>
</div>
