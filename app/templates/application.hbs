<nav class='navbar navbar-default'>
  <div class='container'>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class='navbar-brand'><a href="/">Cat Fancy!</a></div>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <div class='navbar-right'>
        <ul class="nav navbar-nav">
          <li><a href="about">About</a></li>
        </ul>
        {{#if session.isAuthenticated}}
          <div class="sign-buttons nav navbar-nav">
            <button class='btn btn-default navbar-btn' {{action 'signOut'}}>
              {{fa-icon 'sign-out'}} Sign Out
            </button>
          </div>
        {{else}}
          <div class="sign-buttons nav navbar-nav">
            {{#link-to 'sign-in' }}
              <button class="btn btn-default navbar-btn">Sign In</button>
            {{/link-to}}
            {{#link-to 'sign-up'}}
              <button class="btn btn-default navbar-btn">Sign Up</button>
            {{/link-to}}
          </div>
        {{/if}}
      </div>
    </div>
  </div>
</nav>
<div class='container'>
    {{outlet}}
</div>
